#!/usr/bin/env paster

[exe]
command = serve
daemon = true

[filter:prefetch]
use = egg:bsc#prefetch

[pipeline:main]
pipeline = prefetch

[server:main]
use = egg:Paste#http
host = localhost
port = 8000
